# SeaORM CLI Configuration

# Database connection URL
# This will be overridden by DATABASE_URL environment variable if set
database_url = "postgresql://leanwms:leanwms@localhost:5432/leanwms"

# Migration directory
migration_dir = "src/database/migration"

# Entity directory
entity_dir = "src/database/entities"

# Entity file suffix
entity_file_suffix = ".rs"

